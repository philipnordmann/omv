install_packages:
    debian:
        - apt-transport-https
        - ca-certificates
        - curl
        - software-properties-common
        - python3-pip
        - virtualenv
        - python3-setuptools
        - lsb-release

compose_location: /etc/compose